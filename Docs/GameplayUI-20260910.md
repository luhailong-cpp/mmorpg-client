# 背包、任务、活动 UI

2026-09-10，新增三页正式原生 uGUI 和主城入口。主城点击「背包 / 任务 / 活动」，或使用 B / J / O；输入文字时不触发字母快捷键。三个窗口共用一套玉绿、米白细金边、灯笼红绳与月兔饰件，并与人物、宝宝窗口互斥，进战斗或断线时收起。

## 已接入能力

- 背包：读取服务器真实实例与独立布局，按槽位分页，支持名称/编号搜索、装备分类、详情、货币和容量。打开与筛选不整理；只有玩家显式点「整理背包」才发送整理 RPC，服务端返回后刷新布局。
- 任务：真实目录和运行进度、状态筛选、任务分页、多目标分页与本地追踪。选择和追踪使用 `(scope, mission_id)`，避免不同作用域的同号任务串数据。
- 活动：真实活动目录、开放状态筛选、分页与详情；界面节庆装饰不决定活动排期。
- 数据层：复用 GameClient 传输，断线清快照，拒绝旧连接、旧角色和被后发请求覆盖的回包；保留失败前的有效快照并提示重试。

## 协议与素材

正式源为服务端 `proto/scene/player_bag.proto`、`player_mission.proto`、`player_activity.proto`，经正式生成脚本生成 C# 与 handler。新增 190 活动、191 读背包、192 整理、193 任务；原 0–189 不变。

新资源位于 `Assets/Resources/UI/Ugui/GameplayV1`，共 26 张已核对切片，使用新的 GUID，未覆盖旧 UI 资源。细金框采用源九宫格边距；现有物品图标通过 Sprite/Texture2D 兼容加载器读取。

当前任务表和物品表缺少名称、描述、图标展示字段时，线上按编号和中性占位显示，不把编辑器样例伪装成已配置玩法。任务的接取、领奖、持久化尚未完整实现，因此本次 UI 不开放这些写操作；活动 15、16、17 尚未排期，不显示虚构开放时间或领取按钮。

## 验证

使用 Unity 官方 relay MCP 执行编译、生产控件截图与 EditMode 测试。客户端及 UI 回归 **78 通过、0 失败、0 跳过**，包括新客户端 20 项和新原生 UI 行为 9 项。

截图由 `Assets/Editor/GameplayUiVerification.cs` 在独立附加场景渲染正式 GameplayWindow，两种尺寸 2560×1080、1920×1080，共 16 张：三页填充、三页空态、背包加载和失败。截图有编辑器验证水印；角色物品、名称、数量、任务进度和节庆名称均为视觉夹具，不是在线运营配置。

证据在 `.codex-artifacts/gameplay-ui-20260910`：`tests.xml`、`tests-status.txt`、16 张截图、`fixture-scope.txt`；构建与安装结果另由执行记录补充。

服务端全工程串行构建通过，新增接口测试 13/13、完整背包基线 158/158 通过。本次运行新 RPC 需要更新本地 gate 和 scene；其他服务不需要本次重启，没有数据库迁移。服务部署记录在 `E:/work/tmp/features-backend-20260910`。

未使用账号凭据、自动创建角色或初始化永久 ID 水位。真实账号进入主城后的请求验证仍需已有登录环境可用，不能用编辑器夹具截图代替真实联机验收。

## 游戏包交付

最终 Windows 构建：`result=Succeeded errors=0 warnings=0`，输出 `E:/work/tmp/gameplay_ui_player_20260910/mmorpg.exe`。已同步到现有启动入口的默认目录 `E:/work/tmp/showcase_player`，289 个文件 SHA256 全匹配；首轮覆盖前完整备份保留在 `E:/work/tmp/gameplay-ui-20260910/previous-player`。最终安装记录为 `player-install.json`，首次安装记录另存 `player-install-initial.json`。

最后一轮仅调整进度条背景透明度，让 0/3 显示为浅色轨道、3/5 显示部分绿色填充；重新渲染两种尺寸并重新构建、同步。`three-pages-preview.jpg` 为最终三页截图拼表。客户端 `git diff --check` 通过；字体新增字形保留，只清理 Unity 自动序列化产生的行尾空白。
## 服务部署停点

客户端最终包已安装，但 gate/scene 新服务尚未部署。2026-09-10 的 `deploy-features.ps1 -CheckOnly` 调用在创建进程前被自动审批拒绝；没有执行预检脚本，也没有停止、启动或替换任何服务。拒绝理由：脚本具有服务启停能力，而用户当前未明确授权服务更新或重启。需获得本地 gate、scene 更新授权后，重新核对运行身份和零连接守护再执行。

可审阅的具体计划：`E:/work/tmp/features-backend-20260910/deploy-plan.json`；受控脚本：`deploy-features.ps1`。计划包含旧新程序 hash、PID/启动时间、仅两项服务范围、完整备份和原启动环境。原有清单文本替换的静态验证已通过。授权前不执行含启停能力的脚本，也不通过间接方式绕过审批。